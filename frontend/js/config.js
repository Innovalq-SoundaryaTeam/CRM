/**
 * config.js — the ONLY file you need to edit when deploying this frontend
 * somewhere other than your own computer.
 *
 * Locally (served from localhost/127.0.0.1), the app talks to the backend
 * at http://localhost:8000 automatically — you don't need to touch this
 * file for local development.
 *
 * When you deploy the frontend (e.g. to Render, Netlify, GitHub Pages),
 * set DEPLOYED_API_BASE_URL below to your deployed backend's URL (no
 * trailing slash), for example:
 *
 *   const DEPLOYED_API_BASE_URL = "https://candidate-crm-api.onrender.com";
 *
 * Leave it as an empty string only if the frontend and backend are served
 * from the exact same origin (rare in a typical two-service deployment).
 */
const DEPLOYED_API_BASE_URL = "";
